# Самопроверка проекта (self-checklist)


Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.


В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.


> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.


---


## Таблица самопроверки


| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅ | `README.md` разделы «Быстрый старт» и «Запуск в Docker»; `Makefile` команды `make init-kb && make run`; сервер поднимается на `http://localhost:8080` |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅ | Основной endpoint — `POST /api/chat` (`app/app.py`); вызывает `LLMService.generate_response()` → `RAGService.search()` (FAISS + `ai-forever/FRIDA`) → LLM-провайдер (`app/services/rag_service.py`, `app/services/llm_service.py`) |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅ | `app/notebooks/01_eda_knowledge_base.ipynb` — анализ knowledge base (структура, чанки, распределение по категориям); `app/notebooks/02_data_pipeline.ipynb` — pipeline индексации с метриками |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅ | `app/notebooks/03_baseline_comparison.ipynb`|
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅ | `app/services/` — бизнес-логика (rag_service, llm_service); `app/services/providers/` — провайдеры LLM (base, openrouter, gigachat); `app/utils/` — логирование; `app/config.py` — конфигурация; тесты в `app/tests/` |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅ | `Dockerfile` и `docker-compose.yml` в корне репозитория; инструкции `docker build` / `docker-compose up --build` в `README.md`; альтернатива без Docker: `make install && make init-kb && make run` |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅ | `app/.env.example` содержит только имена переменных без значений; `app/.env` в `.gitignore`; все секреты (GIGACHAT_CREDENTIALS, OPENROUTER_API_KEY) передаются только через env |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅ | `GET /api/health` → `{"status": "ok", "timestamp": ..., "version": ...}` (`app/app.py`); структурированное логирование через `app/utils/logger.py`; `logger.info` / `logger.error` на каждый запрос и ошибку |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | `report.md` |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации |  ✅ | `README.md` раздел «Сценарий демонстрации» — 6 конкретных шагов: запуск сервиса, веб-чат, health check, демо-запрос с skin_type, показ sources в ответе, смена провайдера |


---


## Подсчёт баллов и связь с оценкой


За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.  
Максимум – 10 баллов.


Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:


- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).


Окончательное решение по оценке остаётся за преподавателем и может учитывать:


- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.


---